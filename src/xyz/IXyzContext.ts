/**
 * Interface that the context would provide.  `interface` instead of `type`, this is because a context value is not just a set of values but also functions, so `interface` is a more natural use of it.
 * @category Context
 */
export interface IXyzContext {}
